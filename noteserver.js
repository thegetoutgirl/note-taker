// BACK END PACKAGES
const http = require("http");
const fs = require ("fs");
const uuid = require("uuid");
const express = require("express") 

// PORT & SERVER
const PORT = 7017;
// const server = http.createServer(); not sure i need something here?

// GET ROUTE

// POST ROUTE

// DELETE ROUTE

// APP.LISTEN
app.listen(PORT, function() {
    console.log("App listening on PORT " + PORT);
})